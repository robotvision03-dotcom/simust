# storeFile=mysimust-upload.jks
# storePassword=CHANGE_ME
# keyAlias=mysimust
# keyPassword=CHANGE_ME
#
# Create the keystore once with:
#   .\create-upload-keystore.ps1
# Then fill in the real passwords above and save as keystore.properties
# (never commit keystore.properties or *.jks).
